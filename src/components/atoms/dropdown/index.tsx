import Dropdown from "antd/lib/dropdown";
import withDirection from "../../hoc/with-direction";
import withDropdownStyle from "../../hoc/with-dropdown-style";

export default withDirection(withDropdownStyle(Dropdown));
